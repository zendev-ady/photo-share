const config = {
  plugins: ["@tailwindcss/postcss"],
};

export default config;
// This file is used to configure PostCSS, a tool for transforming CSS with JavaScript.
// It specifies that the "@tailwindcss/postcss" plugin should be used.